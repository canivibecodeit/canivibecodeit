{
  "slug": "webhook-site",
  "version": 1,
  "summary": "A webhook inspector you host: mint a URL, catch anything sent to it with headers and body intact, watch requests arrive live, replay one to your localhost, and let bins expire. Nine dollars a month buys permanence and scripting; this buys understanding.",
  "time": "one sitting",
  "stack": [
    {
      "part": "Runtime",
      "choice": "Node 22, node:http and node:sqlite",
      "why": "store and display"
    },
    {
      "part": "Live updates",
      "choice": "A 2-second vanilla-JS poll",
      "why": "no websocket library for a dev tool"
    },
    {
      "part": "Hosting",
      "choice": "A VPS behind Caddy",
      "why": "the catcher must be public"
    }
  ],
  "prerequisites": [
    {
      "id": "node",
      "kind": "tool",
      "name": "Node.js 22 or newer",
      "why": "Everything in this build runs on it: the server, the scripts, the tests.",
      "get": "Download the LTS installer from nodejs.org, or install with your package manager (brew install node, or nvm install 22). Restart the terminal afterwards.",
      "verify": "node --version prints v22 or higher",
      "cost": "free",
      "url": "https://nodejs.org"
    },
    {
      "id": "editor",
      "kind": "tool",
      "name": "A terminal and a code editor",
      "why": "Every step below is a command you type or a file you edit.",
      "get": "VS Code (code.visualstudio.com), Cursor or Zed. Open a folder for the project and use the editor's built-in terminal.",
      "verify": "You can open a folder and run a command in its terminal",
      "cost": "free",
      "url": "https://code.visualstudio.com"
    },
    {
      "id": "git",
      "kind": "tool",
      "name": "Git",
      "why": "History for your code, and the way most hosts deploy.",
      "get": "Install from git-scm.com or with your package manager, then run git init in the project folder once it exists.",
      "verify": "git --version prints a version",
      "cost": "free",
      "url": "https://git-scm.com"
    },
    {
      "id": "vps",
      "kind": "account",
      "name": "A small always-on server (VPS)",
      "why": "This needs one process running all the time with a public address. The URL must be reachable from the services that call it.",
      "get": "Hetzner Cloud (from about 4 EUR), DigitalOcean or Fly.io. Ubuntu 24.04, the smallest size. You need SSH access and a public IP. Only needed for the deploy phase; develop locally first.",
      "cost": "about $5 a month",
      "optional": true,
      "url": "https://www.hetzner.com/cloud"
    },
    {
      "id": "domain",
      "kind": "account",
      "name": "A domain or subdomain",
      "why": "A public address you own, so links you share never break when a provider changes.",
      "get": "Register at Cloudflare Registrar, Porkbun or Namecheap, or use a subdomain of one you already own. You add one DNS record in the deploy phase.",
      "cost": "roughly $10 a year, or free on an existing domain",
      "optional": true,
      "url": "https://porkbun.com"
    },
    {
      "id": "caddy",
      "kind": "tool",
      "name": "Caddy on the server",
      "why": "Automatic HTTPS in front of the Node process. Without TLS the browser features this relies on (and your visitors' trust) do not work.",
      "get": "On the VPS: follow the install steps at caddyserver.com/docs/install for Ubuntu. One Caddyfile with your domain and a reverse_proxy line is the whole config.",
      "verify": "caddy version prints a version on the server",
      "cost": "free",
      "optional": true,
      "url": "https://caddyserver.com/docs/install"
    },
    {
      "id": "sender",
      "kind": "asset",
      "name": "Something that sends webhooks to test with",
      "why": "Phase 1 and 3 need real deliveries.",
      "get": "Stripe test mode, a GitHub repository webhook, or curl.",
      "cost": "free"
    }
  ],
  "env": [
    {
      "name": "PORT",
      "example": "3000",
      "required": true,
      "secret": false,
      "where": "Any free port."
    },
    {
      "name": "DATABASE_PATH",
      "example": "./data/hooks.db",
      "required": true,
      "secret": false,
      "where": "SQLite file."
    },
    {
      "name": "SITE_URL",
      "example": "https://hooks.yourdomain.com",
      "required": true,
      "secret": false,
      "where": "Public base URL."
    },
    {
      "name": "BIN_TTL_DAYS",
      "example": "7",
      "required": false,
      "secret": false,
      "where": "Default expiry for a bin."
    },
    {
      "name": "ADMIN_USER",
      "example": "admin",
      "required": true,
      "secret": false,
      "where": "Any username for the basic-auth admin pages."
    },
    {
      "name": "ADMIN_PASS",
      "example": "change-me-to-a-long-random-string",
      "required": true,
      "secret": true,
      "where": "Generate one: openssl rand -base64 24. Never reuse a real password."
    }
  ],
  "phases": [
    {
      "id": "p1",
      "title": "Catch anything",
      "goal": "Any method, any subpath, stored intact, body capped.",
      "steps": [
        {
          "id": "p1s1",
          "do": "Create the project and tables",
          "detail": "bins (id, created_at, expires_at, note), requests (id, bin_id, received_at, method, path, query, headers JSON, body, content_type, size, ip_hash).",
          "commands": [
            "mkdir hooks && cd hooks && git init && npm init -y && npm pkg set type=module",
            "mkdir -p data && cp .env.example .env"
          ]
        },
        {
          "id": "p1s2",
          "do": "Store any method on /b/:uuid and subpaths; body up to 1 MB, binary as base64; answer 200 with a tiny JSON"
        }
      ],
      "check": [
        "GET, JSON POST, form POST and a binary PUT all store with headers intact",
        "A 2 MB body is refused with 413"
      ]
    },
    {
      "id": "p2",
      "title": "Inspector",
      "goal": "Newest first, detail view, copy as curl, live poll.",
      "steps": [
        {
          "id": "p2s1",
          "do": "/b/:uuid/inspect listing requests and a detail view with pretty JSON and raw body"
        },
        {
          "id": "p2s2",
          "do": "Copy-as-curl and a 2-second poll appending new rows"
        }
      ],
      "check": [
        "A request appears within 2 seconds",
        "JSON pretty-prints",
        "Copy-as-curl reproduces the request"
      ]
    },
    {
      "id": "p3",
      "title": "Replay",
      "goal": "Re-send a stored request to a URL you type.",
      "steps": [
        {
          "id": "p3s1",
          "do": "A replay form posting the stored method, headers and body to a target"
        },
        {
          "id": "p3s2",
          "do": "Show the target's response"
        }
      ],
      "check": [
        "A captured Stripe test event replays to a local server"
      ]
    },
    {
      "id": "p4",
      "title": "Bins and retention",
      "goal": "Mint, expire, prune, rate limit.",
      "steps": [
        {
          "id": "p4s1",
          "do": "A home page that mints a bin; expiry per bin, extendable behind basic auth"
        },
        {
          "id": "p4s2",
          "do": "Nightly prune and a per-IP mint limit"
        }
      ],
      "check": [
        "An expired bin returns 410",
        "Pruning deletes its requests",
        "Minting is rate limited"
      ]
    },
    {
      "id": "p5",
      "title": "Deploy",
      "goal": "HTTPS, service, README.",
      "steps": [
        {
          "id": "p5s1",
          "do": "/healthz, systemd, Caddy"
        },
        {
          "id": "p5s2",
          "do": "README: the body cap, retention, and that anyone with the URL can read the bin",
          "files": [
            "README.md"
          ]
        }
      ],
      "check": [
        "State survives a restart",
        "A stranger can mint a bin from the README's URL"
      ]
    },
    {
      "id": "p6",
      "title": "Operate it like a product",
      "productOnly": true,
      "goal": "Only for the product-builder path: know when the catcher is down, never lose the database, and keep the server patched.",
      "steps": [
        {
          "id": "p6s1",
          "do": "Add a /healthz endpoint and an external uptime check against it",
          "detail": "Answer 200 with the build id and a quick database read. Point a free uptime monitor (or your own, from the Healthchecks entry on this site) at it so an outage is noticed before a user notices."
        },
        {
          "id": "p6s2",
          "do": "Write structured request logs and rotate them",
          "detail": "One JSON line per request: method, path, status, duration, no raw IPs. Rotate weekly with logrotate, keep eight."
        },
        {
          "id": "p6s3",
          "do": "Back the SQLite file up off the machine nightly and test a restore",
          "detail": "SQLite's .backup command makes a consistent copy while the app runs. Copy it to object storage or a second machine; then, once, restore it into a fresh checkout and confirm the app reads it.",
          "commands": [
            "sqlite3 data/app.db \".backup '/tmp/app-$(date +%F).db'\"",
            "rclone copy /tmp/app-$(date +%F).db remote:backups/"
          ]
        },
        {
          "id": "p6s4",
          "do": "Lock the box down",
          "detail": "Firewall allowing only 22, 80 and 443; unattended security updates on; the app running as an unprivileged user under systemd with Restart=on-failure."
        }
      ],
      "check": [
        "Stopping the service triggers an uptime alert within a few minutes",
        "A restore from last night's backup contains yesterday's data",
        "A port scan from another machine shows only 22, 80 and 443"
      ]
    }
  ],
  "product": {
    "outcome": "A team inspector with permanent bins and replay, on your infrastructure.",
    "architecture": [
      {
        "module": "Catcher",
        "owns": "/b routes",
        "swap": "Any listener writing the rows"
      },
      {
        "module": "Inspector",
        "owns": "views and poll",
        "swap": "Websockets later"
      },
      {
        "module": "Replay",
        "owns": "re-sending",
        "swap": "Add scheduled replays"
      }
    ],
    "operations": {
      "backup": "SQLite .backup nightly; bins are mostly disposable.",
      "restore": "Copy back.",
      "monitoring": "Uptime on /healthz.",
      "incident": "A bin receiving secrets by mistake: delete it; rotate the secret."
    },
    "releaseGate": [
      "Body cap and expiry verified",
      "One restore drill performed"
    ]
  },
  "notIncluded": [
    "Custom actions, email hooks, DNS hooks, teams."
  ],
  "after": [
    "A forwarding rule per bin to a real endpoint",
    "Request search"
  ]
}
